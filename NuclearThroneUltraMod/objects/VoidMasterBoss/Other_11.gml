/// @description Execution attack
gunangle = point_direction(x,y,target.x,target.y);
alarm[4] = 11;
targetX = target.x;
targetY = target.y;
if random(2) < 1
	walk = actTime;

if target.x > x
	right = -1
else
	right = 1