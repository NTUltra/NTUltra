event_inherited()

motion_add(direction,constAcc)

if sprite_index == spr_walk && run == 0
	sprite_index = spr_idle;
	

if speed > acc+run
speed = acc+run
else if speed < 0.6
speed = 0.6;


if hspeed > 0
right = 1
else if hspeed < 0
right = -1